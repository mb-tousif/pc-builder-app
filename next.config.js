/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.startech.com.bd", "tecdn.b-cdn.net", "img.freepik.com"],
  },
}

module.exports = nextConfig
